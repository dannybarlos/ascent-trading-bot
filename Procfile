# Procfile for Railway deployment
# This tells Railway how to run different process types

# Web service (main API + frontend)
web: chmod +x railway-start.sh && ./railway-start.sh

# Worker process (optional, for background jobs)
# worker: python -m services.trading_bot

# Release phase (optional, for database migrations)
# release: python -m alembic upgrade head
